// se exporta la función calcularPromedio
export function calcularPromedio(nota1, nota2, nota3) {
    // se calcula el promedio
    let promedio = (nota1 + nota2 + nota3) / 3;
    // se retorna el promedio
    return promedio;
}